# 🧊❄️ Пушистые Льдинки (Fluffy Icicles)

> NFT коллекция от LoveShop на блокчейне Solana

![Solana](https://img.shields.io/badge/Solana-14F195?style=for-the-badge&logo=solana&logoColor=white)
![NFT](https://img.shields.io/badge/NFT-Collection-purple?style=for-the-badge)

## 📖 История создания

В холодную зиму 2023 года команда LoveShop собралась в уютном коворкинге Санкт-Петербурга, чтобы создать что-то по-настоящему уникальное. Вдохновением послужила необычная зима — снежинки были настолько пушистыми, что казалось, будто природа создает живые произведения искусства.

Наш арт-директор Александра заметила, как утренний свет преломляется сквозь ледяные кристаллы на окне, создавая невероятные узоры. Так родилась идея **Пушистых Льдинок** — NFT коллекции, где каждая льдинка обладает уникальным характером и пушистостью.

### Философия проекта

Мы хотели показать, что даже холод может быть теплым и уютным. Каждая льдинка в нашей коллекции — это персонаж со своей историей, настроением и магическими способностями.

## 🎨 О коллекции

- **Размер коллекции:** 3,333 уникальных NFT
- **Блокчейн:** Solana
- **Mint цена:** 0.5 SOL
- **Редкость:** 150+ уникальных черт
- **Категории:** Арктические, Горные, Космические, Мистические

### Уровни редкости

| Уровень | Количество | Особенности |
|---------|------------|-------------|
| Common | 60% | Базовые пушистые льдинки |
| Rare | 25% | Светящиеся эффекты |
| Epic | 12% | Анимированные элементы |
| Legendary | 3% | Уникальные 1/1 дизайны |

## 💻 Техническая реализация

### Стек технологий

- **Metaplex Candy Machine v3** — для минтинга
- **Rust** — смарт-контракты
- **TypeScript** — фронтенд
- **IPFS/Arweave** — хранение метаданных
- **React + Next.js** — веб-интерфейс

### Пример создания NFT метаданных

```typescript
import { Metaplex, keypairIdentity } from "@metaplex-foundation/js";
import { Connection, clusterApiUrl, Keypair } from "@solana/web3.js";

// Подключение к Solana
const connection = new Connection(clusterApiUrl("mainnet-beta"));
const wallet = Keypair.fromSecretKey(/* ваш ключ */);

const metaplex = Metaplex.make(connection)
  .use(keypairIdentity(wallet));

// Создание NFT
async function createFluffyIcicle() {
  const { nft } = await metaplex.nfts().create({
    uri: "https://arweave.net/metadata-hash",
    name: "Пушистая Льдинка #1337",
    sellerFeeBasisPoints: 500, // 5% роялти
    symbol: "FLUFFY",
    creators: [
      {
        address: wallet.publicKey,
        share: 100,
      },
    ],
    collection: {
      address: collectionMint,
      verified: true,
    },
  });

  console.log("NFT создан:", nft.address.toString());
}
```

### Структура метаданных

```json
{
  "name": "Пушистая Льдинка #42",
  "symbol": "FLUFFY",
  "description": "Уникальная пушистая льдинка из морозных глубин севера",
  "image": "https://arweave.net/image-hash.png",
  "attributes": [
    {
      "trait_type": "Пушистость",
      "value": "Экстремальная"
    },
    {
      "trait_type": "Цвет",
      "value": "Северное сияние"
    },
    {
      "trait_type": "Температура",
      "value": "-273°C"
    },
    {
      "trait_type": "Магия",
      "value": "Криогенез"
    },
    {
      "trait_type": "Редкость",
      "value": "Epic"
    }
  ],
  "properties": {
    "files": [
      {
        "uri": "https://arweave.net/image-hash.png",
        "type": "image/png"
      }
    ],
    "category": "image",
    "creators": [
      {
        "address": "LoveSh0pXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxX",
        "share": 100
      }
    ]
  }
}
```

## 🚨 Кейс: Инцидент с дублированием льдинок

### Проблема

**15 марта 2024, 03:47 UTC** — Во время массового минтинга произошел критический инцидент. Из-за проблем с оракулом случайности в Candy Machine, 47 льдинок были заминчены с идентичными метаданными.

### Хронология событий

**03:47** — Обнаружено дублирование метаданных для токенов #1234-#1280

**03:52** — Команда экстренно приостановила минтинг

**04:15** — Анализ показал проблему с VRF (Verifiable Random Function)

**04:45** — Принято решение об исправлении

### Код проблемы

```rust
// Проблемный код (упрощенно)
pub fn mint_nft(ctx: Context<MintNFT>) -> Result<()> {
    let clock = Clock::get()?;
    // ПРОБЛЕМА: использовался только timestamp для генерации индекса
    let random_index = clock.unix_timestamp % metadata_array.len() as i64;
    
    // При одновременных транзакциях получали одинаковый индекс!
    let metadata = &metadata_array[random_index as usize];
    
    // ... остальная логика минтинга
}
```

### Решение

```rust
// Исправленный код
use solana_program::sysvar::slot_hashes;

pub fn mint_nft(ctx: Context<MintNFT>, bump: u8) -> Result<()> {
    let clock = Clock::get()?;
    let slot_hashes = SlotHashes::get()?;
    
    // Используем комбинацию факторов для истинной случайности
    let recent_slothash = slot_hashes.first().unwrap();
    let random_seed = [
        &clock.unix_timestamp.to_le_bytes(),
        &ctx.accounts.payer.key().to_bytes(),
        recent_slothash.0.as_ref(),
        &[bump],
    ].concat();
    
    let random_hash = hash(&random_seed);
    let random_index = u64::from_le_bytes(
        random_hash.to_bytes()[0..8].try_into().unwrap()
    ) % metadata_array.len() as u64;
    
    let metadata = &metadata_array[random_index as usize];
    
    // ... остальная логика минтинга
    Ok(())
}
```

### Действия команды

1. **Немедленная остановка** минтинга
2. **Компенсация держателям** — аирдроп уникальных Legendary льдинок
3. **Обновление контракта** с улучшенной генерацией случайности
4. **Аудит безопасности** от независимой компании
5. **Полная прозрачность** — публикация отчета в блоге

### Результат

- Все пострадавшие получили компенсацию в виде эксклюзивных NFT
- Контракт был обновлен и прошел повторный аудит
- Минтинг успешно завершен через 6 часов
- Сообщество оценило прозрачность команды

Этот инцидент научил нас важности:
- Тщательного тестирования edge cases
- Использования проверенных источников случайности
- Быстрой реакции на проблемы
- Открытой коммуникации с сообществом

## 🛠️ Установка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/loveshop/fluffy-icicles.git
cd fluffy-icicles

# Установить зависимости
npm install

# Настроить переменные окружения
cp .env.example .env

# Запустить локально
npm run dev
```

### Конфигурация Candy Machine

```typescript
import { CandyMachineV3 } from "@metaplex-foundation/mpl-candy-machine";

const candyMachineSettings = {
  itemsAvailable: 3333,
  symbol: "FLUFFY",
  sellerFeeBasisPoints: 500,
  maxEditionSupply: 0,
  isMutable: true,
  creators: [
    {
      address: creatorAddress,
      verified: true,
      share: 100,
    },
  ],
  configLineSettings: {
    prefixName: "Пушистая Льдинка #",
    nameLength: 4,
    prefixUri: "https://arweave.net/",
    uriLength: 43,
    isSequential: false,
  },
};
```

## 🎁 Утилиты для держателей

- **Staking** — получайте $ICE токены за холдинг
- **Breeding** — создавайте новых льдинок (Gen 2)
- **Метаверс** — доступ к зимнему миру LoveShop
- **Мерч** — эксклюзивная одежда для держателей

## 🌐 Ссылки

- **Сайт:** https://fluffyicicles.loveshop.io
- **Twitter:** [@FluffyIciclesNFT](https://twitter.com)
- **Discord:** https://discord.gg/fluffyicicles
- **Marketplace:** [Magic Eden](https://magiceden.io)

## 👥 Команда LoveShop

- **Александра** — Арт-директор и основатель
- **Дмитрий** — Lead Developer (Solana)
- **Мария** — Community Manager
- **Игорь** — 3D Artist

## 📄 Лицензия

MIT License - проект с открытым исходным кодом

---

**Сделано с ❄️ и любовью командой LoveShop**

*"Каждая льдинка уникальна, как и каждый из нас"*
